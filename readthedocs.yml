# .readthedocs.yml
version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.10"

python:
  install:
    - method: pip
      path: ./src
    - requirements: src/requirements.txt

sphinx:
  configuration: docs/conf.py

formats:
  - html
